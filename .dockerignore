# 构建上下文裁剪：依赖与产物在镜像内重新生成，避免把宿主（Windows）的
# node_modules / dist（平台二进制不匹配）和大体积无关文件带进镜像。
.git
.gitignore
**/node_modules
dist
.artifacts
.turbo
.cache
coverage
*.log

# 文档与本地资料，运行不需要
adoc
docs/assets
docs/images
**/*.png
**/*.jpg

# 本地状态 / 凭据，绝不入镜像
.openclaw
.openclaw-dev
**/.env
**/.env.*
**/credentials
