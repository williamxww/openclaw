git clone https://github.com/openclaw/openclaw.git
cd openclaw
pnpm install && pnpm build && pnpm ui:build

创建全局命令 openclaw 
pnpm link --global .
可以用下面的方案代替，因为上面的命令会修改我的node_modules位置
printf '#!/bin/bash\nnode /data/server/oschina/openclaw/openclaw.mjs "$@"\n' > /root/.local/share/pnpm/bin/openclaw
chmod +x /root/.local/share/pnpm/bin/openclaw


openclaw onboard --install-daemon

openclaw plugins install @tencent-connect/openclaw-qqbot@latest --dangerously-force-unsafe-install

openclaw gateway restart
openclaw gateway stop
openclaw gateway status